spring:
  datasource:
    url: jdbc:mysql://localhost:3306/moviemeta
    username: devuser
    password: devuser

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

project:
  poster: poster/

base:
  url: "http://localhost:8080"

mail:
  host: smtp.gmail.com
  port: 587
  username: "moviemeta01@gmail.com"
  password: "Archit@123"
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true